import { ContribuyenteMoral } from './contribuyente-moral';

describe('ContribuyenteMoral', () => {
  it('should create an instance', () => {
    expect(new ContribuyenteMoral()).toBeTruthy();
  });
});
